
function Pagination(props) {

    return(
        <div>
            <span>{props.name}</span>
            <span>{props.age}</span>
            <span>{props.bio}</span>
            <span>{props.email}</span>
    
        </div>
    )
}

export default Pagination